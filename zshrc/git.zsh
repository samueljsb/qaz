alias gpp='git push --set-upstream origin $(git branch --show-current)'
alias gfu='git fu'
alias glrbom='git pull --rebase origin master'

# This assumes the oh-my-zsh git plugin is installed.
# It's a shortcut for 'git checkout $(git_main_branch) && git pull'.
alias gcml='gcm && gl'
